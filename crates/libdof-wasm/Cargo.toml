[package]
name = "libdof-wasm"
version = "0.24.2"
edition = "2024"
authors = ["Oxey", "Asya"]
description = ".dof keyboard layout format parser — WebAssembly/npm build of libdof"
license = "Apache-2.0"
repository = "https://github.com/O-X-E-Y/libdof"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
libdof = { path = "../.." }
wasm-bindgen = "0.2"
serde_json = "1.0"
serde-wasm-bindgen = "0.6"
serde = { version = "1.0", features = ["derive"] }
